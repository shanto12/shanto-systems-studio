# Known Limits

- The current app is static-first and does not use Google AI Studio or Gemini at runtime.
- Hero animation is generated locally from still assets rather than using an external image-to-video service in production.
- Netlify account-level controls are outside the app and must be verified separately before being claimed.
